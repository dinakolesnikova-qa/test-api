import {test, expect} from '@playwright/test';

test('Test name', async ({ request }) => { 
// "test" is a function that defines a test case. 
//  It takes a string as the first argument, which is the name of the test, 
// and an async function as the second argument, which contains the test logic. 
// The async function receives an object with various properties, including request, 
// which is used to make HTTP requests.

//--------------------//

// "request" is a fixture provided by Playwright that allows you to make HTTP requests in your tests.
});

